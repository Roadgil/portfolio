원본 경로: OneDrive `16. Sales Forecast/krp_dashboard_app.html`. 단일 HTML 앱으로 KRP 재고 데이터를 시각화, 저장 시 공유 가능한 스냅샷 HTML로 내보냄.

- **실행 방법**: 서버 없이 브라우저에서 `krp_dashboard_app.html` 파일을 그냥 열면 동작하는 단일 파일 웹앱.
- **필요 패키지**: 없음(순수 HTML/JS, 별도 빌드/설치 과정 없음).
- **외부 의존성**: 데이터는 사용자가 파일 업로드(또는 붙여넣기)로 직접 넣는 구조로 추정 — 이 파일 하나에는 회사 내부 시스템에 대한 실시간 연동은 없음.
- **재사용 시 반드시 고쳐야 할 것**: 없음(파일 하나만 있으면 바로 실행 가능). 다만 "저장(공유링크)" 기능을 실제로 쓰려면 그 저장 대상이 되는 외부 서비스(있다면)의 설정이 별도로 필요할 수 있음 — 이 파일 자체에서는 확인 안 됨.
