# 이 저장소에서 작업할 때

Public 저장소 `Roadgil/portfolio`. 사내 자동화 업무 포트폴리오 전시용이다.

- 루트의 `portfolio_*.html`은 손으로 디자인/카피를 다듬은 소개 페이지다. 오탈자 수정 등 요청받은 범위 밖으로
  대량 재작성하지 않는다. `index.html`이 전체 목록.
- `code/<project-slug>/`는 실사용 중인 사내 자동화 원본 코드를 그대로 가져온 것이다(전시용 사본, README.md에
  카드↔코드 매핑 표 있음). 로직을 임의로 "개선"하거나 리팩터링하지 않는다 — 실제 운영 코드와 달라지면
  포트폴리오로서의 의미가 없어진다.
- 새 프로젝트 코드를 추가할 때: state/cache json, 로그, `.bak` 백업, PDF/Excel 데이터 파일은 넣지 않는다.
  커밋 전 반드시 하드코딩된 비밀번호/토큰/키가 있는지 확인할 것 — 발견되면 반드시 사용자에게 먼저 보고하고
  지시를 받은 뒤에만 처리한다(임의로 마스킹하거나 그대로 커밋하지 않는다).
- 이 저장소는 "편집/커밋/푸시 자동허용, 삭제만 제외" 정책 대상이다. 삭제가 필요하면 반드시 먼저 물어본다.
