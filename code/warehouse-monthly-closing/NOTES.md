원본 경로: OneDrive `14. 창작소/월마감/candela-dashboard`. 월마감 시점 창고 재고/비용 대시보드.

- **실행 방법**: 서버 없이 브라우저에서 `index.html` 파일을 그냥 열면 동작하는 단일 파일 웹앱.
- **필요 패키지**: 없음(순수 HTML/JS).
- **외부 의존성**: `Chart.js`(4.4.1)와 `chartjs-plugin-datalabels`(2.2.0)를 cdnjs.cloudflare.com CDN에서 `<script src>`로 직접 불러옴 — 인터넷 연결이 있어야 그래프가 그려짐(오프라인 환경이면 두 CDN 파일을 로컬에 내려받아 경로를 바꿔야 함).
- **재사용 시 반드시 고쳐야 할 것**: 데이터는 파일 내부에 정적으로 들어있거나 사용자가 업로드하는 구조로 추정(파일 하나 기준) — 최신 데이터로 갱신하려면 원본 데이터 입력 로직을 이 파일 안에서 확인할 것.
